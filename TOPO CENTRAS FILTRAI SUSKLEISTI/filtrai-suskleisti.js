document.querySelectorAll('.Filters-filterGroupContainer-1Wl').forEach(function (filter) {
    var filtras = filter.querySelector('.Filters-filterTitle-1AA');
    $(filtras).click();
});


document.querySelectorAll('.Filters-filterGroupContainer-1Wl').forEach(function(filter) {
    var filtras = filter.querySelector('.Filters-moreItems-wNG ')
    $(filtras).click()
    })